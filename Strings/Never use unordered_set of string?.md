It will lead to O(|s|) time to compute hash and then check if it is there in the hashMap or not!!

Instead create hash and store that in the unordered_set!!
But this needs to be accompanied by double hashing or something similar to avoid collisions and wrong answer!!

Linked with [[D. Good Substrings]]